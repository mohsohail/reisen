import React from "react";
import { View, Text } from "react-native";

interface Props {}

const Interaction: React.FunctionComponent<Props> = () => {
  return (
    <View>
      <Text>bottom nav</Text>
    </View>
  );
};

export default Interaction;
